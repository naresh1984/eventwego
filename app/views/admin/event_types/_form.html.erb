<%= form_for @event_type , :url =>  @event_type.new_record? ? admin_event_types_path: admin_event_type_path,html: {id: 'type',name:'type'}  do |f| %>

 <% if @event_type.errors.any? %>

<% @event_type.errors.each do |name, msg| %>
  <%= content_tag :div, :class => "alert alert-#{name}" do %>
    <a class="close" data-dismiss="alert" href="#">×</a>
    <%= "#{name} #{msg}" %>
  <% end %>
<% end %>

  <% end %>

			<table class="tabtable1">			
			
							
				<tr>
					<td width="123" class="fldlable"><%= f.label "Name" %><span class="red">*</span> </td>
					<td width="309"><%= f.text_field :name %></td>				   
				</tr>
				
				
				<tr>
				<td align="center"></td>
				    <td align="center">
				 <%= f.submit "Save",  :class=>'button' %>&nbsp;<%= link_to 'Back', admin_event_types_path, :class=>"button" %>

</td>
				 
				</tr>			
			</table>


<% end %>
